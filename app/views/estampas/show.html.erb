<div class="panel panel-default">
  <div class="panel-body">
    <div class="row">
      <div class="col-md-4">
        <%= image_tag @estampa.image.url(:medium) %>
        <div class="star-rating" data-score= <%= @avg_review %> ></div>
        <em><%= "#{@reviews.length} reviews" %></em>
        <div class="table-responsive">
          <table class="table">
            <tbody>
              <tr>
                <td><strong>Title:</strong></td>
                <td><%= @estampa.title %></td>
              </tr>
              <tr>
                <td><strong>Description:</strong></td>
                <td><%= @estampa.description %></td>
              </tr>
              <tr>
                <td><strong>Criador:</strong></td>
                <td><%= @estampa.criador %></td>
              </tr>
              <tr>
                <td><strong>Rating:</strong></td>
                <td><%= @estampa.rating %></td>
              </tr>
            </tbody>
          </table>
          <%= link_to "Write a Review", new_estampa_review_path(@estampa) %>
        </div>
      </div>
      <div class="col-md-7 col-md-offset-1">
        <h1 class="review_title"><%= @estampa.title %></h1>
        <p><%= @estampa.description %></p>

        <% if @reviews.blank? %>
          <h3>No reviews just yet, would you like to add the first!</h3>
          <%= link_to "Write Review", new_estampa_review_path(@estampa), class: "btn btn-danger" %>
        <% else %>
          <% @reviews.each do |review| %>
            <div class="reviews">
              <div class="star-rating" data-score= <%= review.rating %> ></div>
              <p><%= review.comment %></p>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%= link_to 'Edit', edit_estampa_path(@estampa) %> |
<%= link_to 'Back', estampas_path %>

<script>
    $('.star-rating').raty({
      path: '/assets/',
      readOnly: true,
      score: function() {
            return $(this).attr('data-score');
    }
  });
</script>